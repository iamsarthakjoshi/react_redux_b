import React from "react";
//import assets from "./../assets/assets";

const TodaysWeather = () => {
  return (
    <div style={todaysWeather}>
      <i class="search icon" />
      We don't have any documents matching your query
    </div>
  );
};

const todaysWeather = {
  zIndex: "-9"
};

export default TodaysWeather;
